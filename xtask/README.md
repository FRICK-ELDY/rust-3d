## 使い方
```
cargo run -p xtask -- layout
cargo run -p xtask -- layout --max-depth 4
cargo run -p xtask -- layout --exclude bin,docs
cargo run -p xtask -- layout --truncate 40
cargo run -p xtask -- layout --respect-gitignore false
```
